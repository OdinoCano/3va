`package.json` copied from Bun's `bench/install` (a create-t3-app: Next.js,
tRPC, drizzle, next-auth, tailwind — 16 dependencies, 10 devDependencies,
hundreds of transitive packages). https://github.com/oven-sh/bun/tree/main/bench/install
Only the scripts/metadata were dropped; dependency ranges are unchanged.
