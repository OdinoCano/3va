Copied verbatim from Bun's `bench/express/express.mjs`
(https://github.com/oven-sh/bun/tree/main/bench/express) so all runtimes are
measured on the same Express 5 hello-world app Bun publishes. Only the
`package.json` differs (Bun's pulls `@types/bun`/`typescript`, not needed here).
