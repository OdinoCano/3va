# test262 (conformance ECMA-262)

## Cómo correrlo

```
./scripts/setup-test262.sh                 # clona tc39/test262 en tests/test262/ (gitignored)
cargo test -p vvva_test --test test262 -- --ignored --nocapture
```

El runner vive en `crates/test/src/test262.rs`. Lee el frontmatter YAML-ish de
cada test a mano (sin dependencia de `serde_yaml`), arma el harness (`assert.js`,
`sta.js`, `includes`, `$262`), y lo corre contra `vvva_js::JsEngine`.

## Estado de cobertura (por área)

| Área | Tests (aprox.) | Estado |
|---|---|---|
| Sync (`language/`, `built-ins/` sin flags especiales) | ~44,000 | Soportado |
| `flags: [async]` (`$DONE`) | 5,624 | Soportado — ver `JsEngine::pump_timers` |
| `flags: [module]` | 832 | Soportado parcialmente (~62% pasan) — ver nota abajo |
| `$262.createRealm` | 284 | Soportado — ver `JsEngine::install_test262_realm_support` |
| `$262.agent` | 112 | Soportado (112/112) — ver `JsEngine::install_test262_agent_support` |
| `intl402/` (ECMA-402, `Intl.*`) | 3,357 | **Soportado: 6,642/6,714 (~99%)** — ver abajo |

### Módulos ES (`flags: [module]`)

3va no usa `v8::Module` nativo — los módulos se ejecutan vía el pipeline
existente de `transpiler::transpile_to_cjs` + `require()`. Esto cubre bien
import/export básico, bindings, namespace objects, pero **no puede** replicar
fielmente la semántica de "early errors" de linking entre módulos (bindings
duplicados vía `export *`, ciclos de importación, etc.) — eso requeriría
`v8::Module` real. Los fallos en esa categoría son reales y se reportan tal
cual (no se esconden con skip) para que el número refleje la cobertura real.

## `intl402/` — Intl.* completo vía V8 nativo + shim pequeño

3va embebe V8 **150.0.0** (crate `v8`), y ese V8 ya trae el `Intl` de ECMA-402 casi
completo (ICU): `Intl.DateTimeFormat`, `Intl.NumberFormat`, `Intl.Collator`,
`Intl.PluralRules`, `Intl.Locale`, `Intl.ListFormat`, `Intl.RelativeTimeFormat`,
`Intl.DisplayNames`, `Intl.Segmenter`, `Intl.DurationFormat` y `Temporal`. La
suite `intl402/` (3,357 archivos) pasa **6,642 / 6,714 (~99%)** con la caja.

Los ~72 fallos restantes son casos límite de **versión de ICU/CLDR** (V8 está
pineado a 150 → determinista). Dos de los bajos-costos se parchan con un shim
JS chico inyectado al startup, `crates/js/src/builtins/intl.rs`:

1. `String.prototype.toLocaleLowerCase/UpperCase` validan **todos** los
   identificadores de locale (`Intl.getCanonicalLocales` lanza `RangeError`).
2. `Intl.getCanonicalLocales` conserva `yes` para las claves de extensión no
   booleanas (`ka`/`kf`/`kr`/`ks`/`kv`) — solo las booleanas lo descartan.

**Restricción dura:** los objetos `Intl.*` no se pueden reemplazar a nivel JS,
porque test262 valida la identidad intrínseca (`name`, `length`, descriptor no
enumerable, no-constructor sin `prototype`, `[[FallbackSymbol]]`). Los
reemplazos del shim son arrows/funciones-no-constructibles con el mismo
`name`/`length`/descriptores. El runner lo amarra con
`INTL402_KNOWN_FAILURES = 72`.

Los fallos conocidos restantes, todos por datos de ICU (no por código 3va):

- `DateTimeFormat`: canonicalización de timezones que ICU fusiona
  (`Canberra`→`Sydney`, `Calcutta`→`Kolkata`, `Etc/GMT`→`UTC`, case-insensitive
  de `America/Argentina/Buenos_Aires`), timezone legacy non-IANA (`ACT`), offset
  con U+2212, fallback `islamic`/`islamic-rgsa`, eras únicas de
  `islamic-civil`, `hourCycle` por defecto h11/h12 y `Temporal`+era.
- `Temporal/*/toLocaleString`: era / hourcycle / `datestyle` (~30).
- `Intl.supportedValuesOf`: requiere que ICU soporte `Etc/GMT+13/+14` (no lo
  soporta), calendars `islamic` sin fallback válido, y numbering systems con
  digit-mapping simple.
- `Locale.getHourCycles`: `en-u-sd-gbeng` debería dar h23 (sigue h12).
- `DurationFormat` digital-style/precision, `FallbackSymbol/per-realm`,
  `NumberFormat/prototype/format/numbering-systems`.

Workflow local: para iterar solo sobre `intl402/` sin correr los ~44k de
`language/`+`built-ins/`, recompilar y usar el helper
`./target/debug/intl402run tests/test262 intl402/...` (bin temporal en
`crates/test/src/bin/intl402run.rs`).

## `$262.createRealm` y `$262.agent`

Ambos implementados en `crates/js/src/lib.rs` y expuestos vía
`JsEngine::install_test262_realm_support` / `install_test262_agent_support`
(llamados desde `crates/test/src/test262.rs` antes de correr cada caso).

- **`createRealm`** crea un `v8::Context` nuevo dentro del MISMO isolate
  (no un `JsEngine` nuevo) — intrínsecos propios (su propio `Array`,
  `Error`, etc.), y comparte el "security token" del contexto que lo pidió
  para que el acceso cross-realm a `realm.global.X` no dispare el
  `TypeError: no access` que V8 lanza por defecto entre contextos con
  tokens distintos. Verificado: `built-ins/Boolean` 99/2 → 101/0,
  `language/types` 207/4 → 211/0, `built-ins/Proxy` 607/0, `built-ins/Symbol`
  192/0.
- **`agent`** es harina de otro costal: cada agente corre en un hilo de SO
  real con su PROPIO isolate (los isolates de V8 no son `Send`, así que no
  se puede simular con tareas async como hace `createRealm`). Lo único que
  cruza threads es el backing store del `SharedArrayBuffer` — V8 ya
  implementa `Atomics.wait`/`notify` correctamente entre isolates una vez
  que comparten el mismo backing store, así que no hubo que reimplementar
  esa sincronización, solo la plomería (`$262.agent.start/broadcast/
  getReport/report/receiveBroadcast/leaving/sleep/monotonicNow`).
  Verificado: **112/112** tests de `$262.agent` pasan (todos viven en
  `built-ins/Atomics`).

## Notas del motor que salieron de este trabajo

- `JsEngine::eval()` ahora usa `v8::TryCatch` y devuelve el error real de V8
  con fase: `"[parse] SyntaxError: ..."` o `"[runtime] TypeError: ..."`. Antes
  devolvía strings genéricos (`"compile error"` / `"execution error"`) sin el
  mensaje real — esto mejora los errores reportados en todo el motor, no solo
  en test262.
- `JsEngine::pump_timers()` — `idle()` solo drena microtasks y tareas de
  platform de V8, nunca avanza la rueda de `setTimeout`/`setInterval`. Un
  loop de test (o cualquier consumidor que necesite esperar timers reales)
  debe llamar `pump_timers()` explícitamente.
