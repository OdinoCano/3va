# 01 — Hello World

The simplest possible 3va script. No permissions required.

## Run

```bash
3va run app.js
```

## What it demonstrates

- Basic script execution with `3va run`
- Node.js compatibility (`process`, `console`)
- No `--allow-*` flags needed for pure computation
