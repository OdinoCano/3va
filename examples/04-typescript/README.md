# 04 — TypeScript

Run TypeScript directly — no `tsc`, no `tsconfig.json` needed.

## Run

```bash
3va run math.ts
```

## What it demonstrates

- 3va transpiles TypeScript on-the-fly (JSX/TS stripped via oxc)
- Interfaces, generics, classes — full TypeScript support
- No build step required
